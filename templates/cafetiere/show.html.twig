{% extends 'base.html.twig' %}

{% block title %}Cafetiere{% endblock %}

{% block body %}
    <h1>Cafetiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cafetiere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cafetiere.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ cafetiere.price }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ cafetiere.brand }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cafetiere_index') }}">back to list</a>

    <a href="{{ path('cafetiere_edit', {'id': cafetiere.id}) }}">edit</a>

    {{ include('cafetiere/_delete_form.html.twig') }}
{% endblock %}
