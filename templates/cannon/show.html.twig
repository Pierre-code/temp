{% extends 'base.html.twig' %}

{% block title %}Cannon{% endblock %}

{% block body %}
    <h1>Cannon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cannon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cannon.name }}</td>
            </tr>
            <tr>
                <th>Power</th>
                <td>{{ cannon.power }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cannon_index') }}">back to list</a>

    <a href="{{ path('cannon_edit', {'id': cannon.id}) }}">edit</a>

    {{ include('cannon/_delete_form.html.twig') }}
{% endblock %}
